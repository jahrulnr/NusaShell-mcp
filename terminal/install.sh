#!/usr/bin/env bash
# Terminal plugin: the MCP server is a standalone Go binary (mcp/server).
# No plugin-local npm install is needed. Rebuild after changing *.go:
#   (cd mcp && go build -o server .)
